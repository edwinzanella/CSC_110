def collectNamesSales():
    namesList = []
    salesList = []
    i = 0
    numAuthors = input(int("How many authors are there?"))
    """
    for i in range(numAuthors):
        listedAuthor = input(str("What is the name of the author?"))
        namesList.append(listedAuthor)
    for i in range(numAuthors):
        listedSales = input(int("How many books has this author sold?"))
        salesList.append(listedSales)
    return namesList, salesList
    """
print(collectNames
